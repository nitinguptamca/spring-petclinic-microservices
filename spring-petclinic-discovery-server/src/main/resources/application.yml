eureka:
  client:
    register-with-eureka: false      
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:8761/discovery-server
  # instance:
   #   hostname: localhost